import { ClaimReviewWorkflow } from "@/components/ClaimReviewWorkflow";

export default function Home() {
  return (
    <main className="repguard-page min-h-screen bg-[var(--cg-bg)] text-[var(--cg-text)]">
      <ClaimReviewWorkflow />
    </main>
  );
}
