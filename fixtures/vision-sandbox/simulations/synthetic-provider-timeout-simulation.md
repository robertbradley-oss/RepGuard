# Synthetic Provider Timeout Simulation

fixtureKey: synthetic-provider-timeout-simulation
fixtureVersion: v1
syntheticStatus: synthetic-only
redactionStatus: synthetic-not-applicable
approvalStatus: metadata-reviewed
packageDistributionStatus: distributable-synthetic-demo
safeForDownloadablePackage: true
safeForDemoMode: true
safeForSelfHostedInstall: true

This synthetic markdown fixture represents an operational timeout limitation for sandbox review.

Expected behavior:

- Return a provider-timeout style limitation.
- Treat timeout as operational only.
- Keep observation, uncertainty signal, limitation, and manual-review guidance separate.
- Do not create an evidence conclusion or customer risk signal from the timeout.
- Do not include provider payloads, raw OCR, request identifiers, stack traces, public URLs, object URLs, data URLs, file URLs, or storage handles.

This fixture is synthetic demo content only.
